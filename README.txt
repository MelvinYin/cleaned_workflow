Getting from_dhcl requires running a bash script, which is probably faster. 

Add labelling of consolidated later as well, maybe. Or wait till we confirm the results. 

Converge has to be the delta=30 form first. 
